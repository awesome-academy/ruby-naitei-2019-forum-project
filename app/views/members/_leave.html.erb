<%= form_for current_user.members.find_by(user_id: current_user.id, sub_forum_id: forum_id),
    html: { method: :delete },
    remote: true do |f| %>
  <%= f.submit t(".leave"), class: "btn btn-primary float-right" %>
<% end %>
